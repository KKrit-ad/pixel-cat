# บรีฟ (แบบข้อความ) — วิธีที่ได้ผลจริงกับ AI

ส่ง **`cat-sheet.txt`** ไปให้ AI พร้อมข้อความข้างล่างนี้ **อย่าขอเป็นรูปภาพ**

---

## ทำไมต้องเป็นข้อความ

โมเดลสร้างภาพวาดพิกเซลอาร์ตให้ตรงกริดไม่ได้ ผลลัพธ์จะเบลอและขนาดไม่ตรงเสมอ แต่ถ้าให้ตอบเป็น**ตารางตัวอักษร** มันจะแม่นยำ 100% เพราะไม่มีขั้นตอนเรนเดอร์ให้ผิดพลาด

## รูปแบบไฟล์

`cat-sheet.txt` คือสไปรต์ชีททั้งใบในรูปข้อความ **45 เฟรม เฟรมละ 22 บรรทัด บรรทัดละ 32 ตัวอักษรพอดี**

```
=== 0 walk 1/4 ===
................................      ← 32 ตัวอักษร
....................#..#F#......
.....###...........#S##FF#......
     ... รวม 22 บรรทัด ...
```

## ตารางสี

| อักษร | สี | ใช้ทำอะไร |
|---|---|---|
| `.` | โปร่งใส | พื้นหลัง |
| `#` | `#2b2f36` | เส้นขอบรอบตัว |
| `F` | `#9aa1a9` | สีขนหลัก |
| `D` | `#6e767f` | ลายสลิดเข้ม |
| `L` | `#c8ced4` | ท้อง/อุ้งเท้าสีอ่อน |
| `S` | `#7f868f` | ขาฝั่งไกล (เงา) |
| `P` | `#e2a2ab` | จมูก |
| `I` | `#b3808a` | ในหู |
| `E` | `#63a07d` | ตาสีเขียว |
| `K` | `#1b211e` | รูม่านตา |
| `W` | `#f2f5f2` | ประกายในตา |
| `T` | `#e88fa0` | ลิ้น |
| `b` `d` `h` | แดง | ลูกบอล (สว่าง/เข้ม/ไฮไลต์) |
| `g` `j` | เขียว | จิ้งจก |

## กติกาที่ห้ามผิด

1. **ทุกบรรทัดต้องยาว 32 ตัวอักษรพอดี** ไม่ขาดไม่เกิน
2. **ทุกเฟรมต้องมี 22 บรรทัดพอดี**
3. **ต้องมีครบทั้ง 45 เฟรม** เรียงลำดับเดิม พร้อมบรรทัด `=== N ชื่อ ===` คั่น
4. **ใช้ได้เฉพาะตัวอักษรในตารางสีข้างบน** ห้ามคิดอักษรใหม่
5. **แถวล่างสุด (แถวที่ 22) คือระดับพื้น** เท้าต้องแตะแถวนี้ในท่าที่ยืนบนพื้น
6. **แมวหันหน้าไปทางขวาเสมอ** โปรแกรมพลิกภาพเองเวลาเดินไปทางซ้าย
7. **เฟรมคู่ `*Blink` ต้องเหมือนเฟรมต้นฉบับทุกตัวอักษร ยกเว้นบรรทัดที่เป็นดวงตา**

## ธรรมเนียมการวาดที่ใช้อยู่

- ท่าที่เคลื่อนที่ (`walk` `run` `crouch`) หัวหันข้าง เห็นตาข้างเดียว ปากยื่นไปหน้า
- ท่าที่หยุดนิ่ง (ท่าอื่น ๆ) หัวหันหน้าเข้าหาคนดู เห็นตาสองข้าง
- ลายทางบนตัวต้องตั้งฉากกับกระดูกสันหลัง (ตัวนอน = ลายตั้ง / ตัวตั้ง = ลายขวาง)
- `climb` กับ `held` ตัวตั้งแนวตั้ง / `ball` เป็นลูกบอล / `gecko` เป็นจิ้งจก ไม่ใช่แมว

## สิ่งที่อยากให้ปรับ

*(เขียนเองตรงนี้)*

## วิธีตอบกลับ

ตอบเป็นข้อความล้วนในรูปแบบเดิมทั้งไฟล์ **ห้ามแนบรูป** ถ้าไฟล์ยาวเกินตอบทีเดียว ให้ทยอยตอบทีละกลุ่มท่าโดยคงหัวข้อ `=== N ... ===` ไว้ทุกครั้ง

---

## เอากลับเข้าแอป

```bash
cd ~/Desktop/pixel-cat
python3 text-to-sheet.py ~/Downloads/cat-sheet-new.txt cat-sheet.png
./use-sheet.sh cat-sheet.png
open PixelCat.app
```

`text-to-sheet.py` จะตรวจให้ทุกบรรทัดก่อน ถ้ามีบรรทัดไหนยาวผิดหรือใช้อักษรแปลกปลอม จะบอกเลขบรรทัดที่ผิดแล้วหยุด ไม่เขียนไฟล์เสีย
